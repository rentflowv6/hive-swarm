services:
  - type: web
    name: hive-swarm
    runtime: python
    plan: free
    buildCommand: "echo 'No build needed — pure stdlib'"
    startCommand: "python3 server.py $PORT"
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"
